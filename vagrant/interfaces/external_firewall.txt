source /etc/network/interfaces.d/*

# The loopback network interface, sw-1 interface
auto lo eth0              
iface lo inet loopback

# sw-1 interface, to application server
iface eth0 inet static         
        address 192.168.0.10
        netmask 255.255.255.0

# Do we need to set up for eth1 to the internet? Check